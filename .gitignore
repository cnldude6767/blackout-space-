# Local preview/build artifacts (headless-render screenshots)
cc-preview.png
cc-*.png
*.preview.png

# Node tooling (used only for local headless screenshots)
node_modules/
package.json
package-lock.json
